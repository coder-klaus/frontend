用 `Koa` 部署静态资源服务器，得用一个第三方库，叫 `koa-static`

```js
import Koa from 'koa'
import koaStatic from 'koa-static'

const app = new Koa()

app.use(koaStatic('./uploads'))
app.use(koaStatic('./public'))

app.listen(3000)
```

