## 新增数据

```sql
-- 表名后边通过小括号标识需要插入的数据
-- VALUES后边通过小括号标识需要插入的值 「 值和健需要一一对应 」
--  在sql中，字符串需要使用引号包裹
--   + 标识符（如表名、列名）：用反引号 `...`
--   + 字符串常量：用单引号 '...' 「 可以使用双引号，但推荐使用单引号 」
INSERT INTO t_singer(name, age) VALUES ('五月头', 25);
```



